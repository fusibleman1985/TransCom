spring:
  application:
    name: TransCom
  datasource:
    driver-class-name: org.postgresql.Driver
#    url: вместо config
#    username:
#    password:

  liquibase:
    enabled: true
    change-log: classpath:db.changelog/changelog-master.xml
  main:
    banner-mode: off
